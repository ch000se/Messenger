package com.ch000se.messenger.core.essentials.resources

/**
 * Base marker interface for all string providers.
 *
 * Each module should define its own string provider by extending this interface
 * and registering it in the Hilt graph.
 */
interface StringProvider
